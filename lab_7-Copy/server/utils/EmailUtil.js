const EmailUtil = {
    send(email, id, token) {
      // console.log(`Giả lập gửi email đến ${email} với Token: ${token}`);
      // Thử đổi trong EmailUtil.js
      console.error("!!!!!!!! ĐÃ CHẠY VÀO ĐÂY !!!!!!!!!");
      return new Promise((resolve) => resolve(true)); // Luôn trả về thành công
    }
  };

  
  module.exports = EmailUtil;